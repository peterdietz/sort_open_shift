<%= form_with(model: score, local: true) do |form| %>
  <% if score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hourOffset %>
    <%= form.number_field :hourOffset, id: :score_hourOffset %>
  </div>

  <div class="field">
    <%= form.label :shiftCount %>
    <%= form.number_field :shiftCount, id: :score_shiftCount %>
  </div>

  <div class="field">
    <%= form.label :recentShifts %>
    <%= form.number_field :recentShifts, id: :score_recentShifts %>
  </div>

  <div class="field">
    <%= form.label :nonRecentShifts %>
    <%= form.number_field :nonRecentShifts, id: :score_nonRecentShifts %>
  </div>

  <div class="field">
    <%= form.label :SHITS %>
    <%= form.number_field :SHITS, id: :score_SHITS %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id, id: :score_employee_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
